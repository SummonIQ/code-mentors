# Coder School

Coder School is a shared workspace for students and mentors. The initial build focuses on the
`/workspace` experience: challenges, mentor notes, current work, and ready-to-use resources.

## Running locally

```bash
cd apps/web-app
npm install
npm run dev
```

Open `http://localhost:3000/workspace` to view the workspace.
